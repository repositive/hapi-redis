{
	"name": "@istar-eldritch/hapi-redis",
	"version": "1.0.0",
	"description": "Hapi plugin that exposes a redis client",
	"main": "dist/index.js",
	"scripts": {
		"setup": "typings i && tsc --outDir dist",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Istar-Eldritch",
	"license": "MIT",
	"devDependencies": {
		"hapi": "^13.5.0",
		"redis": "^2.6.2",
		"typescript": "^1.8.10",
		"typings": "^1.3.2",
		"popsicle": "*"
	},
	"peerDependencies": {
		"redis": "2.x"
	},
	"dependencies": {
		"bluebird": "^3.4.1"
	}
}
